<!DOCTYPE html>
<html lang="en">
<head>
    <title>ex6</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css"
          integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
</head>
<body>
<div class="d-flex align-items-center justify-content-center">
    <img class="mx-auto d-block" alt="Company logo" src='/images/0.png'>

</div>
<div class="row">'
    <div class="col-md-5 mx-auto">
        <div class="text-center">
            <h2>Please Choose a password</h2>
        </div>
        <p class="text-center text-danger" id="password warning"></p>
        <form action="/register-password" method="post" id="passwordForm">
            <div class="form-floating ">
                <div class="input-group">
                <span class="input-group-text">
                    <i class="bi bi-key"></i>
                </span>
                    <input type="password" class="form-control" id="password" name="password" placeholder="Password"
                           required maxlength="32" minlength="3">
                </div>
            </div>
            <div class="form-floating">
                <div class="input-group">
                <span class="input-group-text">
                    <i class="bi bi-key"></i>
                </span>
                    <input type="password" class="form-control" id="confirmPassword" name="confirmPassword"
                           placeholder="Confirm Password"
                           required maxlength="32" minlength="3">
                </div>
            </div>
            <br>
            <div class="text-center">
                <a href="/register" class="btn btn-outline-secondary" type="submit">Back</a>
                <button class="btn btn-primary" type="submit" id="submitForm">Register</button>
            </div>
        </form>

        <script>
            // Wait for the DOM to be fully loaded before adding the event listener
            document.addEventListener('DOMContentLoaded', () => {
                document.getElementById("passwordForm").addEventListener("submit", (event) => {
                    // Get the paragraph element to display the error message
                    const paragraph = document.getElementById('password warning')
                    // Get the values of the passwords fields
                    let password = document.getElementById('password').value;
                    let confirmPassword = document.getElementById('confirmPassword').value;

                    // Check if the passwords matches
                    if (password !== confirmPassword) {
                        // Prevent the form from submitting
                        event.preventDefault();
                        // Update the text content of the paragraph element with the error message
                        paragraph.textContent = 'Passwords do not match, please try again'
                    }
                });
            });
        </script>
        <br>
        <div class="text-center">
            <p>already registered? go to <a href="/">login</a> page</p>
        </div>
    </div>
</div>
</body>
</html>
